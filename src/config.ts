/**
 * 随机一个key
 */
export const DEFAULT_KEY = `default_key_${new Date().getTime().toString()}`;

/**
 * 默认的类型
 */
export const DEFAULT_TYPE = 'any';

/**
 * 默认的对象 key
 */
export const DEFAULT_JSON_KEY = '[key: string]';

/**
 * 默认的数字 key
 */
export const DEFAULT_NUMBER_KEY = '0';

/**
 * 对象的括号类型
 */
export const OPEN_BRACE_TOKEN = 'open-brace-token';
export const CLOSE_BRACE_TOKEN = 'close-brace-token';
